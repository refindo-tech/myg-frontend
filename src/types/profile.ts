export default interface Profile {
    userId: number;
    email: string;
    userLabel: string;
    role: string;
    createdAt: string;
    updatedAt: string;
    profilePicture: string;
    fullName:   string;
}